<h1>Movies</h1>

<ul class="movie-list">
    <% @movies.each_with_index do |movie, index| %>
        <li>
            <%= movie.title %> - <%= movie.rating %> <%= link_to "show", movie_path(movie.id) %>
        </li>
    <% end %>
</ul>